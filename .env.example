# Copy this file to .env in the separate runtime checkout for each environment.
# The deployment workflow injects the immutable image digest; it is not part of
# application runtime configuration.
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000
